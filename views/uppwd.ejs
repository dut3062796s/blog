<!doctype html>
<html>
<head>
    <meta charset="utf-8">
    <link rel="stylesheet" href="/bower_components/bootstrap/dist/css/bootstrap.css"/>
    <style>
        body{
            padding:20px;
        }
    </style>
</head>
<body>

更改 <%=user.username%> 密码:

<form role="form" method="post" action="/user/uppwd">
    <div class="form-group">
        <label >旧密码</label>
        <%if(errors && errors.oldPassword){%>
        <div><%=errors.oldPassword%></div>
        <%}%>
        <input name="oldPassword" placeholder="" />
    </div>
    <div class="form-group">
        <label >用户密码</label>
        <%if(errors && errors.newPassword){%>
        <div><%=errors.newPassword%></div>
        <%}%>
        <input name="newPassword" placeholder="" />
    </div>
    <div class="form-group">
        <label >确认密码</label>
        <%if(errors && errors.confirm){%>
        <div><%=errors.confirm%></div>
        <%}%>
        <input name="confirm" placeholder="" />
    </div>

    <button type="submit" class="btn btn-default">更改密码</button>
</form>
</body>
</html>
